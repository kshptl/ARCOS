// Placeholder validator — Plan 2 will replace with ajv-based validation of
// /pipeline/schemas/*.schema.json against /public/data/*. For bootstrap we
// succeed silently so that `prebuild` does not block the build.
console.log("[validate-data] bootstrap stub — skipping (Plan 2 replaces)");
